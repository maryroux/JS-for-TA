let seconds = 60;
let minutes = 60;
let secInHour = seconds * minutes;
let secInDay = secInHour * 24;
let secInMonth = secInDay * 30;
alert(`Seconds in one hour is ${secInHour}. 
Seconds in one day is ${secInDay}. 
Seconds in one month is ${secInMonth}.`);