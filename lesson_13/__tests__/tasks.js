/*****************task_1********************/
function sum(x, y) {
    return Number((x + y).toFixed(1));
}

/****************task_2*******************/
const userList = ['Nick', 'Kate', 'quest123', 'admin', 'new_user_2'];

module.exports = {sum: sum, userList: userList};